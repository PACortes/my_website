---
title: "Introducción a RStudio"
date: '2021-10-05T21:13:14-05:00'
categories: R
tags:
- R
- RStudio
output:
  blogdown::html_page:
    toc: true
type: book
weight: 20
---
```{r setup, include=FALSE}
knitr::opts_chunk$set(collapse = TRUE)
```

`RStudio` no es `R`. Es un programa que ejecuta `R` y proporciona herramientas adicionales que son útiles al escribir código `R`, algo así como su sistema operativo puede ejecutar un navegador web. 

Este taller asumirá que está utilizando `RStudio` para interactuar con `R`y , aunque todo lo que veremos se puede hacer directament en `R`.

# 1. Instalación de `RStudio`
`RStudio` es gratuito y de código abierto. Haz click [aquí](https://www.rstudio.com/products/rstudio/download/) para descargar la versión que corresponda a tu sitema operativo.

`RStudio` requiere la instalación de `R` (versión 3.0. 1 o superior). Para descargar `R`, haz click [aquí](https://cran.r-project.org/bin/windows/base/) si tu sistema operativo es Windows, y [aquí](https://cran.r-project.org/bin/macosx/) en caso de que sea macOS.

# 2. Las partes de `RStudio`
Cuando abras RStudio, notarás muchas ventanas diferentes, cada una con algunas pestañas. En primera instancia, puede que esto sea abrumador. Afortunadamente. solo necesitaremos usar algunos de estas opciones para lograr los objetivos de este taller.

![](/docencia/MMAD109/Sesion01_1_files/RSTUDIO.png){width=400px height=400px}

## Consola

Es el corazón de `R`y `RStudio`. Aquí escribiremos las lineas de comando para "pedir" cosas a RStudio. En la consola también visualzaremos los resultados de las petición realizada (excepto gráficos). Es importante tener en cuenta que:
  
  + Cuando la consola tiene el cursor *>* significa que le podemos dar comandos para ejecutar.
  + Si es que tiene el símbolo *+* quiere decir que nos falta completar el comando anterior. 
  + El texto en rojo nos avisa y explica, errores en la solicitud realizada.
  
## Script
Es un documento de texto con la extensión de archivo *.R* que puede ser leido por `R`. 

En el script escribiremos y ejecutaremos nuestras líneas de código. Además, en el script podemos escribir notas personales. Es importante tener en cuenta que:

* Para ejecutar una(s) línea(s) de comandos, debemos presionar *<Ctrl + R>* (Windows) o *<Ctrl +Enter>* (Mac). Los resultados se visualizaran en la consola.

* Para geneerar notas personales, las(s) línea(s) de texto y/o códigolaLas deben estar precedidas por el el símbolo *#*.

# 2. Tipos de datos en R

Los tipos de datos de uso más común en R son:

## Numérico
Representan números que tienen una parte decimal o fraccionaria.

```{r}
5

25.3

123890.85544
```

## Cadena de texto
Secuencias de caracteres. Puede contener letras, números, espacios, signos de puntuación y símbolos especiales.

```{r}
"a"

"a"

"Contaminacion"
```

## Factor
Dato numérico representado por una etiqueta.

## Lógico
Sólo tienen dos valores posibles: verdadero (TRUE) y falso (FALSE). Representan si una condición o estado se cumple, es verdadero, o no, es falso.

## Perdido (NA) y Vacío (NULL)
En R, usamos NA para representar datos perdidos, mientras que NULL representa la ausencia de datos. La diferencia entre las dos es que un dato NULL aparece sólo cuando R intenta recuperar un dato y no encuentra nada, mientras que NA es usado para representar explícitamente datos perdidos, omitidos o que por alguna razón son faltantes.


## Verificando el tipo de dato
Podemos verificar si un dato es de un tipo específico con la familia de funciones `is()`. Estas funciones toman como argumento un dato, si este es del tipo que estamos verificando, nos devolverán TRUE y en caso contrario devolverán FALSE



```{r, echo=FALSE}
library(gt)
tabla <- data.frame(
  FU=c("is.integer()","is.numeric","is.character()","is.factor()","is.logical()","is.na()","is.null()" ),
                 TD=c("Entero","Numérico","Cadena de texto","Factor","Lógico","NA","NULL"))

tabla %>%
  gt() %>% 
  cols_label(FU="Función",
             TD= "Tipo de dato") %>%
  tab_style(locations=cells_column_labels(columns=everything()),
            style = list(
              cell_borders(sides="bottom",weight=px(3)),
              cell_text(weight = "bold"))) %>%
  tab_options(table.font.size = px(20L),
              column_labels.font.size = px(25L))
  
  
```

# 3. Operadores



## Operador de asignación
Nos permite asignar datos a un objeto. Para ello podems utilizar los símbolos *<-* o *=*.


```{r}
dos<-2

letra<-"a"
```


Es importante tener en cuenta que:

* Si asignamos un datos a un objetoal que ya habíamos asignado datos, nuestro objeto conserva los datos más recientes.

* Esta operación nos permite “guardar” el resultado de operaciones, de modo que podemos recuperarlos sin necesidad de realizar las operaciones otra vez (Revisar *Environment*).

* A lo largo del taller utilizaremos *<-* por ser característico de R y fácil de reconocer visualmente.

## Operador de concatenación

```{r}
dos_digitos<-c(15, 2)
```

# 4. Estructura de datos en R

## Vector
Colección de uno o más datos del mismo tipo.

```{r}
c(15, 2, 3.3, 5, 8)
```


```{r}
vector_numerio<-c(15, 2, 3.3, 5, 8)
```


